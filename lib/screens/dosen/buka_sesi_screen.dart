// temp
import 'package:flutter/material.dart';

class BukaSesiScreen extends StatelessWidget {
  const BukaSesiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Buka Sesi')),
      body: const Center(
        child: Text('Buka Sesi Screen'),
      ),
    );
  }
}